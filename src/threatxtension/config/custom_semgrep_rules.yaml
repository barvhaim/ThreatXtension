rules:
  ##########################################################################
  # TEST RULE - Detect importScripts usage
  ##########################################################################
  - id: test.import_scripts
    message: "[TEST] importScripts() detected - used to load additional JS files"
    severity: INFO
    languages: [javascript, typescript]
    metadata:
      category: test
      description: Detects importScripts() calls to verify custom rules work
    pattern: importScripts(...)

  ##########################################################################
  # 1) Fake UI overlay / fake login button
  ##########################################################################
  - id: banking.fake_overlay.zindex
    message: Suspicious overlay/button with high z-index—possible fake UI capture.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: ui-overlay
      mitre: [TA0009, T1056]
      owasp: [A02-Cryptographic Failures, A07-Identification and Authentication]
    patterns:
      - pattern-either:
          - pattern: |
              $E.innerHTML = $S;
          - pattern: |
              $P.insertAdjacentHTML($POS, $S);
      - metavariable-regex:
          metavariable: $S
          regex: "<div[^>]*(z-index|position\\s*:\\s*(fixed|absolute))"

  ##########################################################################
  # 2) Form hijack / submit interception + exfiltration
  ##########################################################################
  - id: banking.form_hijack.submit_intercept
    message: Form submit intercepted with exfiltration/redirect—possible credential grab.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: form-hijack
      mitre: [T1185, T1056, T1041]
    patterns:
      - pattern: |
          $F.addEventListener('submit', function($EV, ...){ ... });
      - pattern-inside: |
          function($EV, ...){ ... $EV.preventDefault(...); ... }
      - pattern-either:
          - pattern: |
              fetch($URL, ...);
          - pattern: |
              (new XMLHttpRequest()).open($M, $URL, ...);
          - pattern: |
              navigator.sendBeacon($URL, ...);

  ##########################################################################
  # 3) Credential sniffing — hooks on password fields
  ##########################################################################
  - id: banking.cred_sniff.password_input_hooks
    message: Event hooks on password inputs—may capture credentials.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: credential-access
      mitre: [T1056, T1555]
    pattern-either:
      - pattern: |
          document.querySelectorAll('input[type="password"]').forEach(($N) => { ... });
      - pattern: |
          document.querySelector('input[type="password"]').addEventListener($EV, ...);

  ##########################################################################
  # 4) Extension network hijack (chrome.webRequest)
  ##########################################################################
  - id: banking.ext.webrequest.redirect
    message: chrome.webRequest redirect/modify—can hijack requests (transactions, MFA).
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: extension-hijack
      mitre: [T1110, T1565]
    patterns:
      - pattern: |
          chrome.webRequest.onBeforeRequest.addListener(function($D){ ... return { redirectUrl: $U }; }, $F, $O);

  ##########################################################################
  # 5) Global network sniffing—override XHR/fetch
  ##########################################################################
  - id: banking.net_sniff.override_fetch_xhr
    message: Overriding fetch/XHR prototypes—global sniffing or tampering of requests.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: net-interception
      mitre: [T1041, T1557]
    pattern-either:
      - pattern: |
          const $OF = window.fetch;
          window.fetch = function(...){ ... };
      - pattern: |
          XMLHttpRequest.prototype.open = function(...){ ... };
      - pattern: |
          XMLHttpRequest.prototype.send = function(...){ ... };

  ##########################################################################
  # 6) Automatic transfer / auto-submit transactions
  ##########################################################################
  - id: banking.auto_transfer.silent_payment
    message: Potential silent payment/transfer action without explicit user consent.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: transaction-abuse
      mitre: [T1566, T1499]
    patterns:
      - pattern: |
          fetch($URL, { ... });
      - metavariable-regex:
          metavariable: $URL
          regex: "(transfer|payment|pay|wire|cashout|payout)"
      - pattern-not: |
          fetch($URL, { method: 'GET', ... });

  ##########################################################################
  # 7) MutationObserver used for dynamic hooking of sensitive nodes
  ##########################################################################
  - id: banking.mobserver.dynamic_dom_hook
    message: MutationObserver used to append UI or trigger network send—malicious overlay/exfil?
    severity: WARNING
    languages: [javascript, typescript]
    metadata:
      category: stealth-hooking
      mitre: [T1056, T1027]
    pattern: |
      new MutationObserver(...);

  ##########################################################################
  # 8) Exfiltration channels (sendBeacon / Image.src / fetch GET with secrets)
  ##########################################################################
  - id: banking.exfil.generic_channels
    message: Possible exfiltration via sendBeacon/Image.src/fetch.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: exfiltration
      mitre: [T1041]
    pattern-either:
      - pattern: |
          navigator.sendBeacon($URL, $DATA);
      - pattern: |
          (new Image()).src = $URL;
      - patterns:
          - pattern: |
              fetch($URL, ...);
          - metavariable-regex:
              metavariable: $URL
              regex: "https?://[^\\s]+\\?(.*(pass(word)?|user(name)?|token|otp|iban|account).*)"

  ##########################################################################
  # 9) Disabling/weakening CSP (DOM or extension headers)
  ##########################################################################
  - id: banking.csp.disable_or_weaken
    message: CSP disabled/relaxed—enables untrusted script injection.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: hardening-bypass
      mitre: [T1518, T1562]
    pattern-either:
      - pattern: |
          $M.setAttribute('http-equiv', 'Content-Security-Policy');
      - pattern: |
          chrome.webRequest.onHeadersReceived.addListener(function($D){ ... }, $F, ['blocking', 'responseHeaders']);

  ##########################################################################
  # 10) Dynamic eval / new Function (+ decoding)
  ##########################################################################
  - id: banking.obfuscation.eval_newfunc
    message: Dynamic code execution (eval/new Function) with possible decoding—common in malware.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027, T1059]
    pattern-either:
      - pattern: |
          eval($X);
      - pattern: |
          new Function($X);

  ##########################################################################
  # 11) Credential Theft - localStorage/sessionStorage Access
  ##########################################################################
  - id: credential.theft.storage_access
    message: Accessing localStorage/sessionStorage for credentials—potential credential theft.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: credential-theft
      mitre: [T1555.003, T1539]
      cwe: [CWE-522, CWE-312]
      owasp: [A02-Cryptographic Failures, A07-Identification and Authentication]
      description: Detects access to browser storage APIs that may contain credentials
    pattern-either:
      - patterns:
          - pattern: localStorage.getItem($KEY)
          - metavariable-regex:
              metavariable: $KEY
              regex: "(token|password|pass|pwd|credential|auth|session|jwt|bearer|api[_-]?key|secret)"
      - patterns:
          - pattern: sessionStorage.getItem($KEY)
          - metavariable-regex:
              metavariable: $KEY
              regex: "(token|password|pass|pwd|credential|auth|session|jwt|bearer|api[_-]?key|secret)"
      - pattern: |
          for (let $I = 0; $I < localStorage.length; $I++) {
            localStorage.getItem(...);
          }
      - pattern: |
          Object.keys(localStorage).forEach(...)

  ##########################################################################
  # 12) Credential Theft - Password Field Value Extraction
  ##########################################################################
  - id: credential.theft.password_extraction
    message: Direct extraction of password field values—credential harvesting detected.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: credential-theft
      mitre: [T1056.001, T1555]
      cwe: [CWE-522, CWE-359]
      owasp: [A07-Identification and Authentication]
      description: Detects direct access to password input field values
    pattern-either:
      - pattern: |
          document.querySelector('input[type="password"]').value
      - pattern: |
          document.querySelectorAll('input[type="password"]')
      - pattern: |
          $ELEM.querySelector('input[type="password"]').value
      - pattern: |
          document.getElementById($ID).value
      - patterns:
          - pattern: $FORM.elements[$NAME].value
          - metavariable-regex:
              metavariable: $NAME
              regex: "(password|passwd|pwd|pass)"

  ##########################################################################
  # 13) Credential Theft - Form Data Serialization
  ##########################################################################
  - id: credential.theft.form_serialization
    message: Form data serialization with network transmission—credential exfiltration risk.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: credential-theft
      mitre: [T1056.001, T1041]
      cwe: [CWE-522, CWE-319]
      description: Detects form data collection and transmission patterns
    pattern-either:
      - pattern: new FormData($FORM)
      - pattern: |
          $FORM.querySelectorAll('input')

  ##########################################################################
  # 14) Cookie Stealing - document.cookie Access
  ##########################################################################
  - id: cookie.theft.document_cookie_access
    message: Accessing document.cookie—potential cookie theft for session hijacking.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: cookie-theft
      mitre: [T1539, T1550.004]
      cwe: [CWE-539, CWE-614]
      owasp: [A07-Identification and Authentication, A01-Broken Access Control]
      description: Detects access to document.cookie which may contain session tokens
    pattern-either:
      - pattern: document.cookie
      - pattern: |
          const $C = document.cookie;
      - pattern: |
          let $C = document.cookie;
      - pattern: |
          var $C = document.cookie;

  ##########################################################################
  # 15) Cookie Stealing - Cookie Exfiltration
  ##########################################################################
  - id: cookie.theft.cookie_exfiltration
    message: Cookie data being sent to external server—active cookie theft detected.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: cookie-theft
      mitre: [T1539, T1041]
      cwe: [CWE-539, CWE-319]
      owasp: [A07-Identification and Authentication]
      description: Detects patterns where cookies are extracted and transmitted
    pattern: document.cookie

  ##########################################################################
  # 16) Cookie Stealing - chrome.cookies API Abuse
  ##########################################################################
  - id: cookie.theft.chrome_cookies_api
    message: Using chrome.cookies API to access cookies—potential session hijacking.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: cookie-theft
      mitre: [T1539, T1550.004]
      cwe: [CWE-539]
      description: Detects Chrome extension API usage for cookie access
    pattern-either:
      - pattern: chrome.cookies.get(...)
      - pattern: chrome.cookies.getAll(...)
      - pattern: chrome.cookies.getAllCookieStores(...)

  ##########################################################################
  # 17) C2 Communication - WebSocket Connections
  ##########################################################################
  - id: c2.exfiltration.websocket_connection
    message: WebSocket connection to external server—potential C2 communication channel.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1071.001, T1573]
      cwe: [CWE-940]
      description: Detects WebSocket connections that may be used for C2 communication
    pattern: new WebSocket($URL)

  ##########################################################################
  # 18) C2 Communication - Periodic Beaconing
  ##########################################################################
  - id: c2.exfiltration.periodic_beacon
    message: Periodic network requests detected—potential C2 beaconing behavior.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1071, T1041, T1102]
      cwe: [CWE-940]
      description: Detects periodic network communication patterns typical of C2
    pattern-either:
      - pattern: |
          setInterval(function() { ... }, $INTERVAL)
      - pattern: |
          setInterval(() => { ... }, $INTERVAL)

  ##########################################################################
  # 19) C2 Communication - Base64 Encoded Exfiltration
  ##########################################################################
  - id: c2.exfiltration.base64_encoded_data
    message: Base64 encoding with network transmission—obfuscated data exfiltration.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1041, T1132.001]
      cwe: [CWE-319, CWE-311]
      description: Detects base64 encoding of data before network transmission
    pattern-either:
      - pattern: btoa($DATA)
      - pattern: Buffer.from($DATA).toString('base64')
      - pattern: atob($DATA)

  ##########################################################################
  # 20) C2 Communication - DNS Tunneling
  ##########################################################################
  - id: c2.exfiltration.dns_tunneling
    message: Suspicious DNS-like requests with encoded data—potential DNS tunneling.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1071.004, T1048.003]
      cwe: [CWE-940]
      description: Detects patterns consistent with DNS tunneling for data exfiltration
    patterns:
      - pattern: fetch($URL, ...)
      - metavariable-regex:
          metavariable: $URL
          regex: "https?://[a-zA-Z0-9\\-]+\\.[a-zA-Z0-9\\-]+\\.[a-zA-Z]{2,}/(\\?|#)?[a-zA-Z0-9+/=]{20,}"

  ##########################################################################
  # 21) C2 Communication - Steganography (Image-based)
  ##########################################################################
  - id: c2.exfiltration.image_steganography
    message: Canvas/Image manipulation with network send—potential steganographic exfiltration.
    severity: WARNING
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1027.003, T1041]
      cwe: [CWE-311]
      description: Detects canvas manipulation that may hide data in images
    pattern-either:
      - pattern: |
          $CTX.getImageData(...)
      - pattern: |
          $CANVAS.toDataURL(...)
      - pattern: |
          $CANVAS.toBlob(...)

  ##########################################################################
  # 22) Credential Theft - Clipboard Hijacking
  ##########################################################################
  - id: credential.theft.clipboard_hijack
    message: Clipboard access detected—may capture copied passwords or sensitive data.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: credential-theft
      mitre: [T1115]
      cwe: [CWE-200]
      description: Detects clipboard API usage that may capture sensitive data
    pattern-either:
      - pattern: navigator.clipboard.readText()
      - pattern: navigator.clipboard.read()
      - pattern: document.execCommand('paste')

  ##########################################################################
  # 23) Credential Theft - Keylogger Pattern
  ##########################################################################
  - id: credential.theft.keylogger
    message: Keylogging pattern detected—capturing all keyboard input including passwords.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: credential-theft
      mitre: [T1056.001]
      cwe: [CWE-200, CWE-359]
      owasp: [A07-Identification and Authentication]
      description: Detects keylogger patterns that capture keyboard events
    pattern-either:
      - pattern: |
          document.addEventListener('keydown', ...)
      - pattern: |
          document.addEventListener('keypress', ...)
      - pattern: |
          document.addEventListener('keyup', ...)

  ##########################################################################
  # 24) C2 Communication - Chrome Runtime Messaging to External
  ##########################################################################
  - id: c2.exfiltration.chrome_runtime_external
    message: Chrome runtime messaging to external extensions—potential C2 channel.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1071, T1102]
      cwe: [CWE-940]
      description: Detects external messaging that may be used for C2 communication
    pattern-either:
      - pattern: chrome.runtime.sendMessage($EXT_ID, $MSG, ...)
      - pattern: chrome.runtime.connect($EXT_ID, ...)
      - patterns:
          - pattern: chrome.runtime.sendMessage($EXT_ID, $MSG, ...)
          - metavariable-regex:
              metavariable: $EXT_ID
              regex: "[a-z]{32}"

  ##########################################################################
  # 25) Cookie Theft - IndexedDB Cookie Storage
  ##########################################################################
  - id: cookie.theft.indexeddb_storage
    message: Storing cookies in IndexedDB—potential persistent cookie theft mechanism.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: cookie-theft
      mitre: [T1539, T1005]
      cwe: [CWE-539, CWE-312]
      description: Detects storing of cookie data in IndexedDB for persistence
    pattern-either:
      - pattern: |
          $DB.transaction(...).objectStore(...).add(...)
      - pattern: |
          $DB.transaction(...).objectStore(...).put(...)
      - pattern: |
          indexedDB.open(...)

  ##########################################################################
  # 26) C2 Communication - Fetch with Credentials Include
  ##########################################################################
  - id: c2.exfiltration.fetch_credentials_include
    message: Fetch with credentials:include to external domain—session hijacking risk.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1539, T1041]
      cwe: [CWE-352, CWE-319]
      description: Detects fetch requests that include credentials to external domains
    pattern: fetch($URL, ...)

  ##########################################################################
  # 27) Credential Theft - Chrome Identity API Abuse
  ##########################################################################
  - id: credential.theft.chrome_identity_api
    message: Chrome identity API usage—accessing OAuth tokens or user identity.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: credential-theft
      mitre: [T1528, T1550.001]
      cwe: [CWE-522]
      description: Detects Chrome identity API usage for token theft
    pattern-either:
      - pattern: chrome.identity.getAuthToken(...)
      - pattern: chrome.identity.getProfileUserInfo(...)
      - pattern: chrome.identity.launchWebAuthFlow(...)

  ##########################################################################
  # 28) C2 Communication - Dynamic Script Loading from External Source
  ##########################################################################
  - id: c2.exfiltration.dynamic_script_loading
    message: Dynamic script loading from external source—potential remote code execution.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1059.007, T1105]
      cwe: [CWE-494, CWE-829]
      description: Detects dynamic loading of scripts from external sources
    pattern-either:
      - pattern: document.createElement('script')
      - pattern: |
          document.write('<script src="' + $URL + '"></script>')
      - pattern: |
          $ELEM.innerHTML = '<script src="' + $URL + '"></script>'
      - pattern: eval($CODE)
      - pattern: new Function($CODE)

  ##########################################################################
  # 29) C2 Communication - Hardcoded C2 Server List
  ##########################################################################
  - id: c2.infrastructure.server_list
    message: Hardcoded list of suspicious C2 domains detected—malware infrastructure.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1071, T1583.001]
      cwe: [CWE-506, CWE-940]
      description: Detects arrays of suspicious domains used for C2 communication
    patterns:
      - pattern-either:
          - pattern: |
              const $VAR = [ ..., $URL1, ..., $URL2, ... ];
          - pattern: |
              let $VAR = [ ..., $URL1, ..., $URL2, ... ];
          - pattern: |
              var $VAR = [ ..., $URL1, ..., $URL2, ... ];
      - metavariable-regex:
          metavariable: $URL1
          regex: "https?://[a-z0-9]{20,}\\.[a-z]{2,20}\\.[a-z]{2,10}/"
      - metavariable-regex:
          metavariable: $URL2
          regex: "https?://[a-z0-9]{20,}\\.[a-z]{2,20}\\.[a-z]{2,10}/"

  ##########################################################################
  # 30) URL Hijacking - webNavigation Redirect
  ##########################################################################
  - id: hijacking.url_redirect.webnavigation
    message: webNavigation listener redirecting user navigation—URL hijacking detected.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: url-hijacking
      mitre: [T1185, T1565.002]
      cwe: [CWE-601, CWE-346]
      description: Detects webNavigation API abuse to redirect user browsing
    patterns:
      - pattern: |
          chrome.webNavigation.onBeforeNavigate.addListener(...)
      - pattern-inside: |
          chrome.tabs.update($TAB, { url: $URL })

  ##########################################################################
  # 31) Tracking - Persistent User ID Generation
  ##########################################################################
  - id: tracking.persistent_user_id
    message: Generating persistent tracking ID with crypto—user fingerprinting detected.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: tracking
      mitre: [T1082, T1592.004]
      cwe: [CWE-359, CWE-200]
      description: Detects generation of persistent user identifiers for tracking
    patterns:
      - pattern: crypto.getRandomValues($ARRAY)
      - pattern-inside: |
          chrome.storage.local.set({$KEY: $VALUE}, ...)

  ##########################################################################
  # 32) Data Exfiltration - URL and User Data to C2
  ##########################################################################
  - id: c2.exfiltration.url_and_userid
    message: Sending browsing URL and user ID to external server—active surveillance.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1041, T1213]
      cwe: [CWE-359, CWE-319]
      description: Detects exfiltration of browsing history and user identifiers
    patterns:
      - pattern: |
          fetch($URL, { ..., body: JSON.stringify({..., url: $BROWSING_URL, ...}), ... })
      - pattern-inside: |
          async function $FUNC(...) { ... }

  ##########################################################################
  # 33) C2 Communication - Fallback Server Loop
  ##########################################################################
  - id: c2.infrastructure.fallback_loop
    message: Looping through server list with fallback—resilient C2 infrastructure.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1071, T1008]
      cwe: [CWE-940]
      description: Detects C2 resilience pattern with multiple fallback servers
    patterns:
      - pattern: |
          for (const $SERVER of $SERVER_LIST) {
            ...
            try {
              ...
              const $RESPONSE = await fetch($URL, ...);
              ...
              break;
            } catch ($ERROR) {
              ...
            }
          }

  ##########################################################################
  # 34) URL Hijacking - Dynamic Domain Mapping
  ##########################################################################
  - id: hijacking.dynamic_domain_mapping
    message: Dynamic domain mapping for URL redirection—sophisticated hijacking.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: url-hijacking
      mitre: [T1185, T1071.001]
      cwe: [CWE-601]
      description: Detects dynamic mapping of domains for targeted URL hijacking
    patterns:
      - pattern: |
          const $PARSED = new URL($URL);
      - pattern-inside: |
          const $TARGET = $DOMAINS[$PARSED.hostname];
      - pattern-inside: |
          chrome.tabs.update($TAB, { url: ... })

  ##########################################################################
  # 35) Suspicious - Random String Domain Pattern
  ##########################################################################
  - id: suspicious.random_domain_pattern
    message: Suspicious random-string domain detected—likely malicious infrastructure.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: suspicious-infrastructure
      mitre: [T1583.001]
      cwe: [CWE-506]
      description: Detects domains with random character patterns typical of malware
    pattern-either:
      - patterns:
          - pattern: $URL
          - metavariable-regex:
              metavariable: $URL
              regex: "https?://[a-z0-9]{25,}\\.[a-z]{5,25}\\.[a-z]{2,10}/"
      - patterns:
          - pattern: $URL
          - metavariable-regex:
              metavariable: $URL
              regex: "https?://[a-z]{2,5}[0-9]{2,5}[a-z]{2,5}[0-9]{2,5}.*\\.[a-z]{2,10}/"

  ##########################################################################
  # 36) Obfuscation - Large String Array Pattern
  ##########################################################################
  - id: obfuscation.large_string_array
    message: Large string array detected—common obfuscation technique used by malware.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027.010]
      cwe: [CWE-506]
      description: Detects large arrays of strings used for code obfuscation
    patterns:
      - pattern-either:
          - pattern: const $VAR = ["...", "...", "...", "...", "...", ...];
          - pattern: var $VAR = ["...", "...", "...", "...", "...", ...];
          - pattern: let $VAR = ["...", "...", "...", "...", "...", ...];

  ##########################################################################
  # 37) Obfuscation - Computed Property Access with Concat
  ##########################################################################
  - id: obfuscation.computed_property_concat
    message: Computed property access with string concatenation—obfuscated method call.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027]
      cwe: [CWE-506]
      description: Detects obfuscated method calls using computed properties and concat
    pattern-either:
      - pattern: $OBJ[$PROP.concat(...)](...)
      - pattern: $OBJ["...".concat(...)](...)
      - pattern: $OBJ[`$...`.concat(...)](...)

  ##########################################################################
  # 38) Obfuscation - Nested Template Literal Obfuscation
  ##########################################################################
  - id: obfuscation.nested_template_literals
    message: Deeply nested template literals with property access—code obfuscation detected.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027]
      cwe: [CWE-506]
      description: Detects obfuscated strings using nested template literals
    pattern-either:
      - pattern: |
          `${$OBJ1[$PROP1]}${$OBJ2[$PROP2]}${$OBJ3[$PROP3]}`
      - pattern: |
          `${$OBJ[$PROP1]}${"..."}${$OBJ[$PROP2]}`

  ##########################################################################
  # 39) Obfuscation - Conditional Property Access
  ##########################################################################
  - id: obfuscation.conditional_property_access
    message: Conditional property access with complex ternary—obfuscation technique.
    severity: WARNING
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027]
      cwe: [CWE-506]
      description: Detects obfuscated property access using complex conditionals
    pattern-either:
      - pattern: "$OBJ[!$COND ? $VAL1 : $VAL2]"
      - pattern: "$OBJ[$COND ? $VAL1 : $VAL2][$PROP]"

  ##########################################################################
  # 40) C2 Communication - Async Fetch with JSON Response
  ##########################################################################
  - id: c2.exfiltration.async_fetch_json
    message: Async fetch with JSON parsing—potential C2 command retrieval.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1071.001, T1102]
      cwe: [CWE-940]
      description: Detects async fetch patterns that retrieve and parse JSON commands
    patterns:
      - pattern-either:
          - pattern: (await fetch($URL, ...)).json()
          - pattern: |
              $RESP = await fetch($URL, ...)
              ...
              $DATA = await $RESP.json()
      - pattern-inside: |
          async function $FUNC(...) { ... }

  ##########################################################################
  # 41) Obfuscation - Dynamic Method Name Construction
  ##########################################################################
  - id: obfuscation.dynamic_method_construction
    message: Dynamic method name construction—obfuscated API call.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027]
      cwe: [CWE-506]
      description: Detects dynamically constructed method names to hide API calls
    pattern-either:
      - pattern: |
          $OBJ["...".concat($STR1, $STR2)](...)
      - pattern: |
          $OBJ[$VAR1 + $VAR2](...)
      - pattern: |
          $OBJ[`${$VAR1}${$VAR2}`](...)

  ##########################################################################
  # 42) Obfuscation - Excessive Function Wrapping
  ##########################################################################
  - id: obfuscation.excessive_function_wrapping
    message: Excessive function wrapping detected—code obfuscation pattern.
    severity: WARNING
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027]
      cwe: [CWE-506]
      description: Detects unnecessary function wrapping used for obfuscation
    patterns:
      - pattern: |
          function $FUNC1() {
            return function() {
              return $ARRAY
            }
          }

  ##########################################################################
  # 43) Suspicious - Nullish Coalescing with Array Access
  ##########################################################################
  - id: suspicious.nullish_coalescing_array
    message: Nullish coalescing with complex array access—suspicious control flow.
    severity: WARNING
    languages: [javascript, typescript]
    metadata:
      category: suspicious-pattern
      mitre: [T1027]
      cwe: [CWE-506]
      description: Detects suspicious use of nullish coalescing with array indexing
    pattern: $VAR[$EXPR1] ?? $VAR[$EXPR2]

  ##########################################################################
  # 44) C2 Communication - Dynamic Script Assignment
  ##########################################################################
  - id: c2.code_injection.dynamic_script_assignment
    message: Dynamic script assignment to DOM—potential remote code injection.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: c2-communication
      mitre: [T1059.007, T1105]
      cwe: [CWE-94, CWE-829]
      description: Detects dynamic assignment of fetched code to DOM elements
    patterns:
      - pattern: |
          $OBJ[$PROP]($VAR, $RESPONSE)
      - pattern-inside: |
          $RESPONSE = await fetch(...)

  ##########################################################################
  # 45) Obfuscation - String Indexing with Arithmetic
  ##########################################################################
  - id: obfuscation.string_index_arithmetic
    message: String/array indexing with arithmetic operations—obfuscation technique.
    severity: WARNING
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027]
      cwe: [CWE-506]
      description: Detects obfuscated array access using arithmetic expressions
    pattern-either:
      - pattern: $ARR[$NUM1 - $NUM2]
      - pattern: $ARR[$NUM1 + $NUM2]
      - pattern: $ARR[$NUM1 * $NUM2]
      - pattern: $ARR[$NUM1 & $NUM2]

  ##########################################################################
  # 46) Cryptocurrency Mining - WebAssembly Mining Detection
  ##########################################################################
  - id: crypto.mining.webassembly_miner
    message: WebAssembly instantiation detected—potential cryptocurrency mining.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: cryptocurrency-mining
      mitre: [T1496]
      cwe: [CWE-920]
      owasp: [A06-Vulnerable and Outdated Components]
      confidence: high
      description: Detects WebAssembly usage that may indicate cryptocurrency mining
    pattern-either:
      - pattern: WebAssembly.instantiate(...)
      - pattern: WebAssembly.instantiateStreaming(...)
      - pattern: new WebAssembly.Instance(...)
      - pattern: new WebAssembly.Module(...)

  ##########################################################################
  # 47) Cryptocurrency Mining - Worker Thread Mining
  ##########################################################################
  - id: crypto.mining.worker_thread_mining
    message: Web Worker with intensive computation—potential cryptocurrency mining.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: cryptocurrency-mining
      mitre: [T1496]
      cwe: [CWE-920]
      confidence: high
      description: Detects Web Worker patterns used for cryptocurrency mining
    patterns:
      - pattern: new Worker($URL)
      - pattern-inside: |
          setInterval(..., $INTERVAL)

  ##########################################################################
  # 48) Cryptocurrency Mining - Canvas Fingerprinting for Mining Pool
  ##########################################################################
  - id: crypto.mining.canvas_fingerprinting
    message: Canvas fingerprinting combined with network requests—mining pool connection.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: cryptocurrency-mining
      mitre: [T1496, T1082]
      cwe: [CWE-920, CWE-359]
      confidence: medium
      description: Detects canvas fingerprinting used to identify mining clients
    patterns:
      - pattern: $CANVAS.getContext('2d')
      - pattern-inside: |
          fetch($URL, ...)

  ##########################################################################
  # 49) Keylogging - Input Event Capture with Storage
  ##########################################################################
  - id: keylogging.input_capture_storage
    message: Keyboard input captured and stored—active keylogging detected.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: keylogging
      mitre: [T1056.001]
      cwe: [CWE-200, CWE-359]
      owasp: [A07-Identification and Authentication]
      confidence: high
      description: Detects keylogging with local storage persistence
    patterns:
      - pattern-either:
          - pattern: document.addEventListener('keydown', $HANDLER)
          - pattern: document.addEventListener('keypress', $HANDLER)
      - pattern-inside: |
          chrome.storage.local.set(...)

  ##########################################################################
  # 50) Keylogging - Textarea/Input Value Monitoring
  ##########################################################################
  - id: keylogging.input_value_monitoring
    message: Continuous monitoring of input field values—keylogging behavior.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: keylogging
      mitre: [T1056.001]
      cwe: [CWE-200]
      confidence: high
      description: Detects continuous monitoring of input fields for keylogging
    patterns:
      - pattern: setInterval($FUNC, $INTERVAL)
      - pattern-inside: |
          document.querySelectorAll('input, textarea')

  ##########################################################################
  # 51) Screen Capture - MediaDevices Screen Capture
  ##########################################################################
  - id: screen.capture.getdisplaymedia
    message: Screen capture via getDisplayMedia—recording user screen activity.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: screen-capture
      mitre: [T1113]
      cwe: [CWE-359]
      confidence: high
      description: Detects screen recording using MediaDevices API
    pattern-either:
      - pattern: navigator.mediaDevices.getDisplayMedia(...)
      - pattern: navigator.getDisplayMedia(...)

  ##########################################################################
  # 52) Screen Capture - Chrome Tab Capture API
  ##########################################################################
  - id: screen.capture.chrome_tab_capture
    message: Chrome tabCapture API usage—recording browser tab content.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: screen-capture
      mitre: [T1113]
      cwe: [CWE-359]
      confidence: high
      description: Detects Chrome extension tab capture for screen recording
    pattern-either:
      - pattern: chrome.tabCapture.capture(...)
      - pattern: chrome.tabCapture.getCapturedTabs(...)

  ##########################################################################
  # 53) Screen Capture - Canvas Screenshot with Exfiltration
  ##########################################################################
  - id: screen.capture.canvas_screenshot_exfil
    message: Canvas-based screenshot with network transmission—screen capture exfiltration.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: screen-capture
      mitre: [T1113, T1041]
      cwe: [CWE-359, CWE-319]
      confidence: high
      description: Detects canvas screenshots being sent to external servers
    patterns:
      - pattern: $CANVAS.toDataURL(...)
      - pattern-inside: |
          fetch($URL, ...)

  ##########################################################################
  # 54) Clipboard Manipulation - Clipboard Write Hijacking
  ##########################################################################
  - id: clipboard.manipulation.write_hijack
    message: Clipboard write operation—may replace copied data (e.g., crypto addresses).
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: clipboard-manipulation
      mitre: [T1115, T1565.002]
      cwe: [CWE-345]
      confidence: high
      description: Detects clipboard write operations that may hijack copied data
    pattern-either:
      - pattern: navigator.clipboard.writeText($DATA)
      - pattern: navigator.clipboard.write($DATA)
      - pattern: document.execCommand('copy')

  ##########################################################################
  # 55) Clipboard Manipulation - Cryptocurrency Address Replacement
  ##########################################################################
  - id: clipboard.manipulation.crypto_address_replace
    message: Clipboard monitoring with regex matching—cryptocurrency address hijacking.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: clipboard-manipulation
      mitre: [T1115, T1565.002]
      cwe: [CWE-345]
      confidence: high
      description: Detects clipboard hijacking targeting cryptocurrency addresses
    patterns:
      - pattern: navigator.clipboard.readText()
      - pattern-inside: |
          $TEXT.match($REGEX)
      - pattern-inside: |
          navigator.clipboard.writeText($REPLACEMENT)

  ##########################################################################
  # 56) Cookie Theft - Service Worker Cookie Access
  ##########################################################################
  - id: cookie.theft.service_worker_access
    message: Service Worker accessing cookies—persistent cookie theft mechanism.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: cookie-theft
      mitre: [T1539, T1550.004]
      cwe: [CWE-539]
      confidence: high
      description: Detects Service Worker cookie access for persistent theft
    patterns:
      - pattern: self.addEventListener('fetch', ...)
      - pattern-inside: |
          $REQUEST.headers.get('Cookie')

  ##########################################################################
  # 57) Cookie Theft - Cookie Synchronization to External Server
  ##########################################################################
  - id: cookie.theft.cookie_sync_external
    message: Periodic cookie synchronization to external server—active cookie theft.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: cookie-theft
      mitre: [T1539, T1041]
      cwe: [CWE-539, CWE-319]
      confidence: high
      description: Detects periodic cookie exfiltration patterns
    patterns:
      - pattern: setInterval($FUNC, $INTERVAL)
      - pattern-inside: |
          chrome.cookies.getAll(...)
      - pattern-inside: |
          fetch($URL, ...)

  ##########################################################################
  # 58) WebSocket Abuse - Persistent WebSocket Connection
  ##########################################################################
  - id: websocket.abuse.persistent_connection
    message: Persistent WebSocket with reconnection logic—C2 communication channel.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: websocket-abuse
      mitre: [T1071.001, T1573]
      cwe: [CWE-940]
      confidence: high
      description: Detects persistent WebSocket connections with reconnection logic
    patterns:
      - pattern: new WebSocket($URL)
      - pattern-inside: |
          $WS.onclose = function() { ... }

  ##########################################################################
  # 59) WebSocket Abuse - Binary Data Transmission
  ##########################################################################
  - id: websocket.abuse.binary_transmission
    message: WebSocket binary data transmission—potential encrypted C2 communication.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: websocket-abuse
      mitre: [T1071.001, T1132]
      cwe: [CWE-940]
      confidence: medium
      description: Detects binary data transmission over WebSocket
    patterns:
      - pattern: $WS.send($DATA)
      - pattern-inside: |
          $WS.binaryType = 'arraybuffer'

  ##########################################################################
  # 60) LocalStorage Exfiltration - Bulk Storage Enumeration
  ##########################################################################
  - id: storage.exfiltration.localstorage_bulk_enum
    message: Bulk localStorage enumeration—exfiltrating all stored data.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: storage-exfiltration
      mitre: [T1005, T1041]
      cwe: [CWE-312, CWE-319]
      confidence: high
      description: Detects bulk enumeration of localStorage for data exfiltration
    patterns:
      - pattern-either:
          - pattern: |
              for (let $I = 0; $I < localStorage.length; $I++) { ... }
          - pattern: |
              Object.keys(localStorage).forEach(...)
      - pattern-inside: |
          fetch($URL, ...)

  ##########################################################################
  # 61) SessionStorage Exfiltration - Session Data Theft
  ##########################################################################
  - id: storage.exfiltration.sessionstorage_theft
    message: SessionStorage data extraction with network transmission—session theft.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: storage-exfiltration
      mitre: [T1539, T1041]
      cwe: [CWE-312, CWE-319]
      confidence: high
      description: Detects sessionStorage data exfiltration
    patterns:
      - pattern: sessionStorage.getItem($KEY)
      - pattern-inside: |
          fetch($URL, { ..., body: $DATA, ... })

  ##########################################################################
  # 62) DOM Manipulation - Fake Banking Login Form Injection
  ##########################################################################
  - id: dom.manipulation.fake_login_injection
    message: Injecting fake login form into banking pages—phishing attack.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: dom-manipulation
      mitre: [T1185, T1056]
      cwe: [CWE-1021]
      owasp: [A07-Identification and Authentication]
      confidence: high
      description: Detects injection of fake login forms for credential phishing
    patterns:
      - pattern: $ELEM.innerHTML = $HTML
      - metavariable-regex:
          metavariable: $HTML
          regex: "<form[^>]*(password|login|signin)"

  ##########################################################################
  # 63) DOM Manipulation - Payment Form Overlay
  ##########################################################################
  - id: dom.manipulation.payment_form_overlay
    message: Creating overlay payment form—payment information phishing.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: dom-manipulation
      mitre: [T1185, T1056]
      cwe: [CWE-1021]
      confidence: high
      description: Detects creation of fake payment forms to steal financial data
    patterns:
      - pattern: document.createElement('div')
      - pattern-inside: |
          $ELEM.style.position = 'fixed'
      - metavariable-regex:
          metavariable: $ELEM
          regex: "(payment|card|cvv|billing)"

  ##########################################################################
  # 64) Network Request Interception - Fetch Override with Modification
  ##########################################################################
  - id: network.interception.fetch_override_modify
    message: Fetch API override with request modification—MITM attack on network requests.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: network-interception
      mitre: [T1557, T1565.002]
      cwe: [CWE-300]
      confidence: high
      description: Detects fetch API override to modify network requests
    patterns:
      - pattern: |
          const $ORIG = window.fetch;
          window.fetch = function($URL, $OPTS) { ... };
      - pattern-inside: |
          $OPTS.body = $MODIFIED

  ##########################################################################
  # 65) Network Request Interception - XHR Response Tampering
  ##########################################################################
  - id: network.interception.xhr_response_tamper
    message: XMLHttpRequest response tampering—modifying server responses.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: network-interception
      mitre: [T1557, T1565.002]
      cwe: [CWE-300]
      confidence: high
      description: Detects XHR response interception and modification
    patterns:
      - pattern: XMLHttpRequest.prototype.open = function(...) { ... }
      - pattern-inside: |
          this.addEventListener('load', ...)

  ##########################################################################
  # 66) Extension Message Hijacking - Runtime Message Interception
  ##########################################################################
  - id: extension.hijacking.runtime_message_intercept
    message: Chrome runtime message interception—hijacking extension communication.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: extension-hijacking
      mitre: [T1557, T1071]
      cwe: [CWE-940]
      confidence: high
      description: Detects interception of Chrome extension messages
    patterns:
      - pattern: chrome.runtime.onMessage.addListener($HANDLER)
      - pattern-inside: |
          chrome.runtime.sendMessage($EXT_ID, $MODIFIED_MSG)

  ##########################################################################
  # 67) Extension Message Hijacking - Port Communication Hijack
  ##########################################################################
  - id: extension.hijacking.port_communication_hijack
    message: Chrome port communication hijacking—intercepting extension messages.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: extension-hijacking
      mitre: [T1557, T1071]
      cwe: [CWE-940]
      confidence: high
      description: Detects hijacking of Chrome extension port communication
    patterns:
      - pattern: chrome.runtime.connect($EXT_ID)
      - pattern-inside: |
          $PORT.onMessage.addListener(...)

  ##########################################################################
  # 68) Cryptocurrency Mining - High CPU Usage Loop
  ##########################################################################
  - id: crypto.mining.high_cpu_loop
    message: Intensive computation loop—potential cryptocurrency mining activity.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: cryptocurrency-mining
      mitre: [T1496]
      cwe: [CWE-920]
      confidence: medium
      description: Detects intensive computation patterns typical of crypto mining
    patterns:
      - pattern: |
          while (true) { ... }
      - pattern-inside: |
          Math.random()

  ##########################################################################
  # 69) Keylogging - Form Autofill Interception
  ##########################################################################
  - id: keylogging.autofill_interception
    message: Intercepting form autofill events—capturing autofilled credentials.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: keylogging
      mitre: [T1056.001, T1555]
      cwe: [CWE-200]
      confidence: high
      description: Detects interception of browser autofill to capture credentials
    pattern-either:
      - pattern: $ELEM.addEventListener('input', ...)
      - pattern: $ELEM.addEventListener('change', ...)

  ##########################################################################
  # 70) Screen Capture - Periodic Screenshot Capture
  ##########################################################################
  - id: screen.capture.periodic_screenshot
    message: Periodic screenshot capture—continuous screen surveillance.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: screen-capture
      mitre: [T1113]
      cwe: [CWE-359]
      confidence: high
      description: Detects periodic screen capture for surveillance
    patterns:
      - pattern: setInterval($FUNC, $INTERVAL)
      - pattern-inside: |
          chrome.tabs.captureVisibleTab(...)


  ##########################################################################
  # 71) Cryptocurrency Mining - WebAssembly Mining Detection
  ##########################################################################
  - id: crypto.mining.webassembly_mining
    message: WebAssembly instantiation with worker threads—potential cryptocurrency mining.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: cryptocurrency-mining
      mitre: [T1496]
      cwe: [CWE-920]
      confidence: high
      description: Detects WebAssembly-based cryptocurrency mining using worker threads
    patterns:
      - pattern-either:
          - pattern: WebAssembly.instantiate($BUFFER, ...)
          - pattern: WebAssembly.instantiateStreaming($SOURCE, ...)
      - pattern-inside: |
          new Worker(...)

  ##########################################################################
  # 72) Cryptocurrency Mining - Worker Thread Mining Pool Connection
  ##########################################################################
  - id: crypto.mining.worker_pool_connection
    message: Worker thread connecting to mining pool—cryptocurrency mining detected.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: cryptocurrency-mining
      mitre: [T1496]
      cwe: [CWE-920]
      confidence: high
      description: Detects worker threads connecting to known mining pool domains
    patterns:
      - pattern: new Worker($SCRIPT)
      - pattern-inside: |
          new WebSocket($URL)
      - metavariable-regex:
          metavariable: $URL
          regex: "(coinhive|cryptoloot|jsecoin|minero|webminer|pool\\.)"

  ##########################################################################
  # 73) Ad Injection - DOM Manipulation for Ad Insertion
  ##########################################################################
  - id: ad.injection.dom_manipulation
    message: Suspicious DOM manipulation for ad injection—inserting iframes/scripts.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: ad-injection
      mitre: [T1189, T1059.007]
      cwe: [CWE-79]
      confidence: high
      description: Detects DOM manipulation patterns used for ad injection
    patterns:
      - pattern-either:
          - pattern: document.body.appendChild($ELEM)
          - pattern: document.body.insertBefore($ELEM, ...)
          - pattern: $PARENT.insertAdjacentHTML('beforeend', $HTML)
      - metavariable-regex:
          metavariable: $HTML
          regex: "(<iframe|<script|doubleclick|googlesyndication|adserver)"

  ##########################################################################
  # 74) Ad Injection - Script Tag Injection
  ##########################################################################
  - id: ad.injection.script_tag_injection
    message: Dynamic script tag creation with ad network URLs—ad injection attack.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: ad-injection
      mitre: [T1189, T1059.007]
      cwe: [CWE-79]
      confidence: high
      description: Detects injection of script tags pointing to ad networks
    patterns:
      - pattern: |
          $SCRIPT = document.createElement('script');
          ...
          $SCRIPT.src = $URL;
      - metavariable-regex:
          metavariable: $URL
          regex: "(doubleclick|googlesyndication|adnxs|advertising|adserver|popads)"

  ##########################################################################
  # 75) Browser Hijacking - Homepage Override
  ##########################################################################
  - id: browser.hijacking.homepage_override
    message: Chrome settings override to change homepage—browser hijacking.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: browser-hijacking
      mitre: [T1176, T1547.001]
      cwe: [CWE-912]
      confidence: high
      description: Detects attempts to override browser homepage settings
    pattern-either:
      - pattern: "chrome.settings.homepage.set({ value: $URL })"
      - pattern: "chrome.browserSettings.homepageOverride.set({ value: $URL })"

  ##########################################################################
  # 76) Browser Hijacking - Search Engine Override
  ##########################################################################
  - id: browser.hijacking.search_engine_override
    message: Search engine settings override—hijacking default search provider.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: browser-hijacking
      mitre: [T1176, T1547.001]
      cwe: [CWE-912]
      confidence: high
      description: Detects attempts to override default search engine
    pattern-either:
      - pattern: chrome.settings.searchProvider.set(...)
      - pattern: chrome.search.setDefault(...)

  ##########################################################################
  # 77) Browser Hijacking - New Tab Override
  ##########################################################################
  - id: browser.hijacking.newtab_override
    message: New tab page override detected—browser hijacking to control new tabs.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: browser-hijacking
      mitre: [T1176, T1547.001]
      cwe: [CWE-912]
      confidence: high
      description: Detects manifest or API calls to override new tab page
    patterns:
      - pattern: "chrome.tabs.create({ url: $URL })"
      - pattern-inside: |
          chrome.tabs.onCreated.addListener(...)

  ##########################################################################
  # 78) Advanced Keylogging - Keyboard Event Capture with Timing
  ##########################################################################
  - id: keylogging.advanced.keyboard_timing
    message: Advanced keylogging with keystroke timing analysis—sophisticated credential theft.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: keylogging
      mitre: [T1056.001]
      cwe: [CWE-200]
      confidence: high
      description: Detects advanced keylogging with timing analysis for keystroke dynamics
    patterns:
      - pattern: document.addEventListener('keydown', $HANDLER)
      - pattern-inside: |
          $HANDLER = function($EVENT) {
            ...
            $TIMESTAMP = Date.now()
            ...
          }

  ##########################################################################
  # 79) Advanced Keylogging - Clipboard Access and Monitoring
  ##########################################################################
  - id: keylogging.advanced.clipboard_monitoring
    message: Clipboard monitoring detected—capturing copied passwords and sensitive data.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: keylogging
      mitre: [T1115]
      cwe: [CWE-200]
      confidence: high
      description: Detects clipboard monitoring to steal copied credentials
    pattern-either:
      - pattern: document.addEventListener('copy', ...)
      - pattern: document.addEventListener('cut', ...)
      - pattern: document.addEventListener('paste', ...)
      - pattern: navigator.clipboard.readText()

  ##########################################################################
  # 80) Obfuscation Detection - Base64 Encoded Eval
  ##########################################################################
  - id: obfuscation.base64_eval
    message: Base64 encoded code with eval—obfuscated malicious code execution.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027, T1059.007]
      cwe: [CWE-506]
      confidence: high
      description: Detects Base64 encoded strings being decoded and executed
    patterns:
      - pattern-either:
          - pattern: eval(atob($ENCODED))
          - pattern: eval(Buffer.from($ENCODED, 'base64').toString())
          - pattern: Function(atob($ENCODED))()

  ##########################################################################
  # 81) Obfuscation Detection - String Concatenation Obfuscation
  ##########################################################################
  - id: obfuscation.string_concatenation
    message: Excessive string concatenation—code obfuscation to evade detection.
    severity: WARNING
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027]
      cwe: [CWE-506]
      confidence: medium
      description: Detects obfuscation through excessive string concatenation
    patterns:
      - pattern: $STR = $A + $B + $C + $D + $E
      - metavariable-regex:
          metavariable: $STR
          regex: "(eval|Function|XMLHttpRequest|fetch|chrome\\.)"

  ##########################################################################
  # 82) Obfuscation Detection - Hex Encoded Strings
  ##########################################################################
  - id: obfuscation.hex_encoding
    message: Hex encoded strings with execution—obfuscated code execution.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027]
      cwe: [CWE-506]
      confidence: high
      description: Detects hex-encoded strings being decoded and executed
    patterns:
      - pattern: eval($HEX.replace(/\\x/g, ''))
      - metavariable-regex:
          metavariable: $HEX
          regex: "\\\\x[0-9a-fA-F]{2}"

  ##########################################################################
  # 83) C2 Communication - WebSocket Command and Control
  ##########################################################################
  - id: c2.communication.websocket_c2
    message: WebSocket connection to suspicious domain—potential C2 communication channel.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: command-and-control
      mitre: [T1071.001, T1573]
      cwe: [CWE-940]
      confidence: high
      description: Detects WebSocket connections used for C2 communication
    patterns:
      - pattern: new WebSocket($URL)
      - pattern-inside: |
          $WS.onmessage = function($EVENT) {
            ...
            eval($EVENT.data)
            ...
          }

  ##########################################################################
  # 84) C2 Communication - Long Polling for Commands
  ##########################################################################
  - id: c2.communication.long_polling
    message: Long polling pattern detected—potential C2 command retrieval mechanism.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: command-and-control
      mitre: [T1071.001]
      cwe: [CWE-940]
      confidence: medium
      description: Detects long polling patterns used for C2 communication
    patterns:
      - pattern: |
          setInterval(function() {
            ...
            fetch($URL)
            ...
          }, $INTERVAL)
      - metavariable-regex:
          metavariable: $INTERVAL
          regex: "[1-9][0-9]{4,}"

  ##########################################################################
  # 85) C2 Communication - DNS Tunneling
  ##########################################################################
  - id: c2.communication.dns_tunneling
    message: Suspicious DNS queries with encoded data—potential DNS tunneling for C2.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: command-and-control
      mitre: [T1071.004]
      cwe: [CWE-940]
      confidence: high
      description: Detects DNS tunneling used for covert C2 communication
    patterns:
      - pattern: fetch($URL)
      - metavariable-regex:
          metavariable: $URL
          regex: "https?://[a-zA-Z0-9]{20,}\\."

  ##########################################################################
  # 86) Advanced Obfuscation - Code Packing Detection
  ##########################################################################
  - id: obfuscation.code_packing
    message: Packed/compressed code detected—obfuscation to hide malicious functionality.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: obfuscation
      mitre: [T1027.002]
      cwe: [CWE-506]
      confidence: high
      description: Detects code packing/compression used for obfuscation
    pattern-either:
      - pattern: eval(function($P,$A,$C,$K,$E,$D){...})
      - pattern: |
          function $UNPACK($P,$A,$C,$K,$E,$D) {
            ...
            return $P.replace(...)
          }

  ##########################################################################
  # 87) Browser Hijacking - Proxy Settings Override
  ##########################################################################
  - id: browser.hijacking.proxy_override
    message: Proxy settings override—redirecting traffic through attacker-controlled proxy.
    severity: CRITICAL
    languages: [javascript, typescript]
    metadata:
      category: browser-hijacking
      mitre: [T1090, T1557]
      cwe: [CWE-300]
      confidence: high
      description: Detects attempts to override browser proxy settings
    pattern-either:
      - pattern: "chrome.proxy.settings.set({ value: $CONFIG })"
      - pattern: chrome.proxy.onProxyError.addListener(...)

  ##########################################################################
  # 88) Ad Injection - Iframe Overlay Injection
  ##########################################################################
  - id: ad.injection.iframe_overlay
    message: Invisible iframe overlay injection—clickjacking or ad injection attack.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: ad-injection
      mitre: [T1189, T1056]
      cwe: [CWE-1021]
      confidence: high
      description: Detects injection of invisible iframes for clickjacking or ads
    patterns:
      - pattern: |
          $IFRAME = document.createElement('iframe');
          ...
          $IFRAME.style.opacity = '0';
      - pattern-inside: |
          document.body.appendChild($IFRAME)

  ##########################################################################
  # 89) Cryptocurrency Mining - CPU Throttling Evasion
  ##########################################################################
  - id: crypto.mining.throttle_evasion
    message: CPU usage throttling evasion—mining while avoiding detection.
    severity: ERROR
    languages: [javascript, typescript]
    metadata:
      category: cryptocurrency-mining
      mitre: [T1496, T1562.001]
      cwe: [CWE-920]
      confidence: medium
      description: Detects techniques to evade CPU usage monitoring while mining
    patterns:
      - pattern: |
          setInterval($FUNC, $INTERVAL)
      - pattern-inside: |
          if (document.hidden) {
            ...
            clearInterval($TIMER)
          }

  ##########################################################################
  # 90) Advanced Keylogging - Mouse Movement Tracking
  ##########################################################################
  - id: keylogging.advanced.mouse_tracking
    message: Mouse movement and click tracking—behavioral biometrics collection.
    severity: WARNING
    languages: [javascript, typescript]
    metadata:
      category: keylogging
      mitre: [T1056.002]
      cwe: [CWE-200]
      confidence: medium
      description: Detects mouse tracking for behavioral profiling
    pattern-either:
      - pattern: document.addEventListener('mousemove', $HANDLER)
      - pattern: document.addEventListener('mousedown', $HANDLER)
      - pattern: document.addEventListener('mouseup', $HANDLER)
